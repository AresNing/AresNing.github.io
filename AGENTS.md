# 博客维护约定

- 修改主题、写文章或生成分享图片前，阅读 `design/paper-v1/design-system.md` 的写作与分享规范；该文档是本站样式规则的权威来源。
- 使用 `scaffolds/post.md` 创建文章。公开前补齐标题、摘要、导读和 kind；引用保留来源，日期必须可确认。学习笔记不得冒充已验证教程。
- 沿用现有 Hexo、paper 主题、成熟交互组件和原生控件；新增 UI 同时适配深浅色与手机。
- 既有文章改标题时保持永久链接。空白草稿保留源文件，使用 `published: false` 隔离。
- 执行 `npm run verify`；涉及交互与视觉时还需真实浏览器验收。构建检查不能替代公开平台分享预览。
- `public/` 为构建产物，根目录旧静态文件不是开发入口。未经明确授权不执行 push 或 deploy。
